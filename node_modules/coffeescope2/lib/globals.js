// Generated by CoffeeScript 1.10.0
(function() {
  "use strict";
  var globals;

  globals = require("globals");

  module.exports = function(envs, custom) {
    var append, env, i, len, result;
    if (envs == null) {
      envs = [];
    }
    if (custom == null) {
      custom = {};
    }
    if (typeof envs === "string") {
      envs = [envs];
    }
    result = {};
    append = function(src) {
      var key, results, value;
      results = [];
      for (key in src) {
        value = src[key];
        results.push(result[key] = result[key] || value);
      }
      return results;
    };
    for (i = 0, len = envs.length; i < len; i++) {
      env = envs[i];
      append(globals[env] || {});
    }
    append(custom);
    result["this"] = false;
    return result;
  };

}).call(this);
